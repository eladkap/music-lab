class Sound {
    constructor(midiNumber, frequency, velocity) {
        this.midiNumber = midiNumber;
        this.frequency = frequency;
        this.velocity = velocity;
    }

    display() {
        
    }
}